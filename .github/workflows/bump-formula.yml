name: Bump Formula

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version (e.g., 0.1.0 or v0.1.0)'
        required: true
      endpoint_url:
        description: 'Default APPCHECK_ENDPOINT_URL for brew wrapper'
        required: true
      posthog_host:
        description: 'Default APPCHECK_POSTHOG_HOST for brew wrapper'
        required: true
      posthog_key:
        description: 'Default APPCHECK_POSTHOG_KEY for brew wrapper'
        required: true

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Update formula
        env:
          VERSION: ${{ inputs.version }}
          ENDPOINT_URL: ${{ inputs.endpoint_url }}
          POSTHOG_HOST: ${{ inputs.posthog_host }}
          POSTHOG_KEY: ${{ inputs.posthog_key }}
        run: |
          chmod +x .github/scripts/update-formula.sh
          .github/scripts/update-formula.sh

      - name: Commit and push
        run: |
          VERSION="${{ inputs.version }}"
          VERSION="${VERSION#v}"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Formula/appcheck.rb
          git commit -m "appcheck ${VERSION}"
          git push
